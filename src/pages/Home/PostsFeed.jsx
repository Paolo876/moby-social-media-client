import React from 'react'
import { Paper, Typography, List, ListItem, ListItemButton, Stack, Divider, Button } from "@mui/material"

const PostsFeed = () => {
  return (
    <List sx={{m:.5, mt: 2, p: 1, height: "100%" }}>
    
    </List>
  )
}

export default PostsFeed