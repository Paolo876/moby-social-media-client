import { useState, useEffect } from 'react'
import axios from 'axios'

const useImagekit = () => {
  
  const uploadImage = ({ authenticationEndpoint, file, fileName, folder, useUniqueFileName=true,  }) => {
    
  }
  return {}
}

export default useImagekit